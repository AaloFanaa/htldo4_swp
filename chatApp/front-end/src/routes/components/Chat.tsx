import { useState } from 'react';
import styles from '../../styles/Chat.module.css';

function Chat() {
  return (
    <div className={styles.Chat}>
      <span>Chat</span>
    </div>
  );
}

export default Chat;
